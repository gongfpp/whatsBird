# whatsBird 开发计划与实现方案

2026-09-13。当前项目目录为空，本轮只制定方案，尚未创建应用、接入模型或完成真机验证。用户已确认首版平台为 Android App，识别范围为中国大陆常见鸟类。下文的鸟种数量、性能指标、保存方式和排期均为建议，不能视为已经实现或验证的能力。

建议采用 Kotlin、Jetpack Compose、CameraX 与 LiteRT，围绕“打开相机 → 同时发现多只鸟 → 分别显示中文种名 → 拍照保存”完成首版。第一阶段先验证模型能否在目标手机上区分实际鸟种，再投入完整界面和功能开发。相机和文字叠加已有成熟的系统接口；最大不确定性是远处小目标的检测、相似鸟种的区分，以及模型在手机上的速度。

本方案暂将“全景扫描”理解为持续分析当前摄像头整个取景范围，用户转动手机时分析新画面，不涉及多张照片拼接或 360° 场景重建。建议先支持后置摄像头、竖屏全屏预览、点击对焦和变焦；底部中央放置拍照按钮，旁边显示最近一张照片缩略图。中国大陆常见鸟类仍需形成有版本的具体清单，建议从 30–50 种开始，先用 10–15 种具有代表性和易混淆关系的鸟验证模型，再决定可交付清单。鸟种数量不应通过加入未经验证的中文标签扩充。

识别链路为 `相机帧 → 多目标鸟类检测 → 分配或更新跟踪 ID → 从原始分析帧裁剪每只鸟 → 鸟种分类 → 多帧稳定判断 → 位置转换与标签绘制`。检测模型负责找出每只鸟的位置，分类模型分别判断这些鸟的种类；同一画面可以出现多只同种鸟，也可以出现多个不同鸟种。普通目标检测器输出的 `bird` 只代表发现鸟，不能当成完成鸟种识别。检测和分类分离，也方便后续独立提升小目标检测效果或扩充鸟种。

鸟类检测以 EfficientDet-Lite0 / Lite2 作为首轮候选基线，先比较漏检、误检、推理耗时与模型输入尺寸带来的细节损失；这只是实验起点，不能预先认定通用模型足以处理树枝间的小鸟。鸟种分类优先评估可部署的现成专项权重；若覆盖或精度不足，再用 EfficientNet-Lite 等轻量骨干针对目标鸟种进行迁移训练，并比较量化前后的结果。BioCLIP 2 可作为电脑端的物种识别对照基线，其模型卡说明它使用 ViT-L/14，但本方案尚未证明它适合手机实时运行，也不会把对照模型输出当作正确答案。候选模型必须在同一组真实样本上比较，最终由精度、时延、模型体积和运行兼容性共同决定。[Google 目标检测模型说明](https://developers.google.com/edge/mediapipe/solutions/vision/object_detector)、[图像分类说明](https://developers.google.com/edge/mediapipe/solutions/vision/image_classifier)、[BioCLIP 2 模型卡](https://huggingface.co/imageomics/bioclip-2)。

训练和验证资料需要包含正面、侧面、背面、幼鸟、逆光、遮挡、模糊以及同种外观差异，同时加入未收录鸟种、树叶和其他动物。每张资料保留来源、鸟种依据和图像使用许可，按观察事件或拍摄序列划分训练、验证与测试集，避免连续帧同时进入训练和测试导致结果偏高。iNaturalist 可以作为候选来源，但照片许可需要逐张筛选，平台的观察记录许可和照片许可不能混用；优先考虑与预期用途匹配的许可或自采数据。[iNaturalist 内容许可说明](https://help.inaturalist.org/en/support/solutions/articles/151000175695)。

实时预览和识别使用不同频率。预览优先保持流畅，检测初始目标为每秒 5–10 次；分类只在新目标出现、画面质量改善、结论不稳定或缓存到期时调度，同屏多鸟轮流处理，避免每帧对每只鸟重复运行模型。CameraX 使用 `STRATEGY_KEEP_ONLY_LATEST` 丢弃来不及分析的旧帧，分析资源在完成后及时释放，推理任务不占用主线程。先建立 CPU 基线，再实测 GPU 等加速方式，不能把开启加速等同于一定更快。模型建议随首版安装包提供，让核心扫描功能无需联网；LiteRT 负责端侧推理，依赖版本在实际开发时锁定并验证。[CameraX 图像分析](https://developer.android.com/media/camera/camerax/analyze)、[LiteRT Android 文档](https://developers.google.com/edge/litert/android)。

每只鸟的实时状态至少包含 `trackId`、位置框、候选种名、内部评分、帧时间戳和最后出现时间。跟踪先采用位置与运动预测匹配；分类结果做短时平滑，切换种名要求更充分的证据，避免标签在两个名字之间跳动。异步结果同时绑定帧时间戳和跟踪代次，鸟已离开、跟踪对象已重建或结果过期时丢弃，避免把旧名字贴到新鸟身上。短暂遮挡只在限定时间内维持预测，完全丢失后移除标签；首版不承诺两只高度相似的鸟长时间互相遮挡后仍保持原身份。

正常标签显示中文种名，例如“白头鹎”；证据不足时显示“识别中”，无法可靠细分时显示“鸟类，暂未确定”。内部评分需要在验证集上校准，并结合候选差距、画面质量和多帧一致性判断，不能把模型分数直接当成真实正确率。清单外鸟种需要作为独立测试项，防止模型强行从已知清单中挑一个名字。鸟太小时提示靠近或调整变焦，数字放大本身不能恢复原始画面中不存在的羽毛细节。

“头上文字框”有两种不同的实现成本：将文字放在鸟的检测框上方，可以先实现鸟上方悬浮文字的视觉效果；若要求鸟转身、低头或横向飞行时仍精确锚定头部，就需要额外的鸟头检测或关键点模型及对应数据。建议把检测框上方作为首版近似方案，并在评审时明确它不等于精确头部定位；严格头部定位保留为待确认范围。标签需要平滑移动、避让其他文字、保持在屏幕内，位置偏移时用短引线指向目标。模型输入、分析图像、预览和拍照图像有不同坐标，统一处理旋转、缩放、裁剪和变焦，避免画面边缘或切换方向后文字偏离。[CameraX 坐标转换](https://developer.android.com/media/camera/camerax/transform-output)。

拍照使用 CameraX `ImageCapture` 获取静态照片。建议一次点击先可靠保存原图，再生成带标签副本，分别标明保存状态；原图和副本都保存会增加相册文件数量，这是当前建议而非用户已确认的偏好。标签副本应以实际拍到的照片重新检测、分类和定位，不直接把较早的预览标签画在新照片上，否则移动中的鸟可能错位。拍照后复核的名字可以与预览不同，应以该照片证据为准；复核失败时仍保留原图，并明确说明标签副本未完成。通过 `MediaStore` 写入相册，建议首版最低支持 Android 10，以简化存储兼容；保存成功必须验证文件可读、方向正确且在系统相册可见。[CameraX 拍照](https://developer.android.com/media/camera/camerax/take-photo)、[Android 共享媒体存储](https://developer.android.com/training/data-storage/shared/media)。

应用按职责划分为相机控制、帧处理、鸟类检测、鸟种分类、目标跟踪、标签叠加、拍照保存和鸟种字典。实时目标只保存在内存，不需要为每帧建立数据库；照片记录可以保存对应文件 URI、种名、模型版本及照片坐标，便于复核。拒绝相机权限时提供可理解的说明与恢复入口，退到后台释放相机，模型加载失败时仍允许普通拍照，磁盘空间不足或保存失败时保留明确失败状态。首版聚焦扫描、标注和拍照，不将账号、社区、地图和鸟类百科计入排期。

建议按下面顺序推进。工作量以单人具备 Android 与模型集成能力、能够使用目标真机和合法样本为前提，属于初步估算；阶段一完成后再形成可靠排期。资料不足、需要专门训练或要求精确头部定位时，模型工作可能成为独立的数周任务，需要先明确成本再扩大开发。

| 阶段 | 主要工作和交付物 | 初步工作量与进入下一阶段的条件 |
| --- | --- | --- |
| 1. 识别可行性验证 | 建立代表鸟种清单和独立样本；比较检测、分类候选；制作最小相机验证程序，在目标手机记录识别错误、延迟和发热 | 3–5 个工作日用于得出可行性结论；交付模型对比、失败样例、端侧数据和明确的可支持范围，而非承诺这几天完成模型训练 |
| 2. 相机与保存基础 | 建立 Android 工程、全屏预览、权限、对焦变焦、快门和原图保存 | 2–3 个工作日；真机打开即预览，拍下照片在系统相册可见 |
| 3. 多鸟实时标注 | 接入可用模型、裁剪分类、目标跟踪、中文标签、多帧稳定、坐标映射和标签避让 | 4–6 个工作日；使用真实多鸟画面验证每只鸟的标签对应关系和移动稳定性 |
| 4. 带标签照片与交付验证 | 静态照片重新识别和标签合成；保存异常、持续运行、相机生命周期及机型性能验证 | 3–4 个工作日；交付可安装 APK、验证记录、已支持鸟种清单和已知限制 |

模型能够复用时，首版工程预估合计约 12–18 个工作日；如果需要补数据、专项训练或精确鸟头定位，应另估，不能把这部分隐藏在界面开发时间里。当前还没有可用鸟种模型的实测证据，因此该工期不能作为交付承诺。阶段一发现模型不能可靠满足范围时，应带着具体失败样例讨论缩小鸟种范围、补数据训练或评估云端增强；不默认引入云服务费用，也不以只有通用“鸟类”标签的演示代替种类识别。

验收应同时衡量“找到鸟”“名字正确”“标签跟得住”和“照片保存成功”。建议先指定一台目标中端手机和一台性能较弱手机；在目标手机正常光照、1–3 只清晰可见的鸟场景下，预览目标为不低于 24 FPS，首个稳定种名延迟的 P95 不高于 1 秒，5 只同框另测性能和错配情况。对独立测试集，建议以清晰主体检测召回率至少 90%、已显示具体种名的结果正确率至少 90%、可识别样本中显示具体种名的覆盖率至少 70% 作为讨论起点，并单独记录清单外鸟被误报为已知种名的比例，初始目标不超过 10%。这些阈值都需要在阶段一根据样本难度和真机结果确认；统计单位用独立目标或视频片段，避免用重复帧虚增样本量。

小鸟、逆光、枝叶遮挡、两鸟交叉、空画面及无鸟画面分别报告，不能把困难样本排除后声称全场景准确。至少进行 15 分钟持续扫描检查预览、发热和掉帧，覆盖退后台再回来、变焦、拍照瞬间移动、连续拍照、拒绝权限和保存失败。使用已标注视频可重复验证识别，但最终还必须用手机摄像头拍摄真实鸟类，屏幕翻拍和离线照片跑分只能算阶段证据。

下一步最有价值的交付是阶段一的真机可行性报告与最小验证程序。开始开发前还需落实目标手机型号，并确认检测框上方的标签近似是否满足“头上”的要求，以及相册默认保存原图、标签图还是两份；本轮不启动应用开发、下载训练数据或购买服务。
