# 公开仓库发布检查

检查日期：2026-09-14。范围为本次提交的源码快照和新建 Git 仓库的全部提交历史；未将本地训练资料、相机照片或工具记忆视为公开内容。

| 检查项 | 处理与结果 |
| --- | --- |
| 原有 Git 历史 | 项目原先没有 `.git` 或嵌套 Git 仓库；从脱敏后的当前源码建立初始提交 |
| 密钥与凭据 | 使用 Gitleaks 8.30.1 默认规则扫描待提交快照，0 项发现；启用编码内容和最多 2 层归档检查 |
| 个人电脑路径 | 移除文档和训练脚本中的绝对个人目录；文档链接改为相对路径，训练解释器改由 `PY` 指定 |
| 局域网与设备地址 | 删除实际局域网地址；安装工具改为显式设备参数或 `ANDROID_SERIAL`，缺少目标时停止并给出说明 |
| 本地资料与构建产物 | `.gitignore` 排除工具记忆、机器配置、训练数据与中间权重、拍摄照片、诊断截图、原始测试日志、APK、缓存及签名材料；这些本地文件未被删除 |
| 随包二进制 | 保留模型、Gradle Wrapper 和 36 张公开来源的测试图片；测试图片全部按文件 SHA-256 匹配来源，均无 EXIF 数据，已补充逐张署名与许可记录 |
| 提交身份 | 使用 GitHub 账号名及 GitHub noreply 邮箱，不使用本机全局配置中的私人邮箱 |
| 防止再次带入 | `.gitleaks.toml` 在默认密钥检测规则外增加个人主目录及私有 IPv4 地址规则；未添加扫描豁免 |

安装脚本的“显式序列号／环境变量／缺少序列号”三条路径已经隔离验证，没有执行真实安装；Python 源码通过语法检查，训练 Shell 脚本通过 `bash -n`。

推送前对初始提交执行完整历史扫描，并核对公开仓库可见性、远端 `main` 与本地提交一致。后续提交可重复运行：

```sh
gitleaks git . --config .gitleaks.toml --redact=100 --ignore-gitleaks-allow --log-opts="--all"
```

这里的“0 项发现”仅指本次规则、人工复核和扫描范围内没有检测到密钥等问题。App 的功能验收结论仍见 [整改复验报告](ACCEPTANCE_RECHECK_2026-09-14.md)，本次发布准备没有修复其中的实时扫描闪退或模型质量问题。
